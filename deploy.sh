#!/bin/bash

# Активируем SSH-агент (если нужно)
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Проверяем статус
echo "🔍 Проверка изменений..."
git status

# Добавляем все изменения
echo "📦 Добавление файлов..."
git add .

# Коммитим с сообщением
echo "📝 Коммит..."
git commit -m "✨ Обновление: $(date +'%d-%m-%Y %H:%M')"

# Подтягиваем изменения с GitHub
echo "🔄 Подтягиваем изменения..."
git pull --rebase

# Отправляем на GitHub
echo "🚀 Отправка на сервер..."
git push

echo "✅ Готово! Сайт обновлён."
